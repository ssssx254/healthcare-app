import {
  fixtureClinics,
  fixtureDoctors,
  fixtureScheduleSlots,
  fixtureServices,
} from "@/data/healthcare";

/** @deprecated Шууд `fixtureClinics` гэх мэт ашиглахыг зөвлөж байна. */
export const mockClinicDetails = fixtureClinics;
export const mockDoctors = fixtureDoctors;
export const mockServices = fixtureServices;
export const mockSlots = fixtureScheduleSlots;
